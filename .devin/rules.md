**Mandatory**: 
 - ALWAYS use the rules defined in the `rules/` directory starting from the file general-guidelines.md
 - use agent skills inside the `.agents/` directory
 - avoid verbose output and only give compact description of the final result