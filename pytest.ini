[pytest]
# Repo-root marker. Anchors pytest's rootdir at the Ideable repo root so the root-level
# conftest.py (the test-runner guard) is loaded for EVERY pytest invocation, regardless
# of which modules/*/TESTS path is targeted. Kept intentionally minimal — it must not
# change collection/behaviour, only fix the rootdir. Framework-owned; do not remove.
